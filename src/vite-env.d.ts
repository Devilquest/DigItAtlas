/// <reference types="vite/client" />

/** The identifier the export stamped into the data, injected at build time from the catalog. */
declare const __DATA_BUILD__: string;

/** The application's own version, injected at build time from package.json. */
declare const __APP_VERSION__: string;
